function Houston(array1, array2, num) {
    
    let houston = [];
    
    array1.sort(), array2.sort();

    for (let i = 0; i < num; i++) {
        total[i] = array1[i] * array2[i];
    }
    return houston;

    
}